name: Mobile Visual Regression Gate V1

on:
  pull_request:
    branches: [main]
    paths:
      - 'src/pages/**'
      - 'src/components/**'
      - 'src/layouts/**'
      - 'src/styles/**'
      - 'src/data/**'
      - 'scripts/qa-mobile-visual-regression-v1.mjs'
      - '.github/workflows/mobile-visual-regression-v1.yml'
  push:
    branches: [main]
    paths:
      - 'src/pages/**'
      - 'src/components/**'
      - 'src/layouts/**'
      - 'src/styles/**'
      - 'src/data/**'
      - 'scripts/qa-mobile-visual-regression-v1.mjs'
      - '.github/workflows/mobile-visual-regression-v1.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  mobile-visual-regression:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
          package-manager-cache: false

      - name: Install Korean font
        run: |
          sudo apt-get update
          sudo apt-get install -y fonts-noto-cjk

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Install Playwright
        run: |
          npm install --no-save --package-lock=false playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start preview
        shell: bash
        run: |
          set -euo pipefail
          npm run preview -- --host 127.0.0.1 --port 4321 > /tmp/mobile-visual-preview.log 2>&1 &
          echo $! > /tmp/mobile-visual-preview.pid
          for i in {1..30}; do
            if curl -fsS http://127.0.0.1:4321/about >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/mobile-visual-preview.log
          exit 1

      - name: Mobile Visual Regression Gate
        run: node scripts/qa-mobile-visual-regression-v1.mjs

      - name: Stop preview
        if: always()
        shell: bash
        run: |
          if [ -f /tmp/mobile-visual-preview.pid ]; then kill "$(cat /tmp/mobile-visual-preview.pid)" || true; fi

      - name: Upload screenshots and report
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: mobile-visual-regression-v1
          path: qa-artifacts/mobile-visual-regression-v1
          if-no-files-found: error
          retention-days: 30
