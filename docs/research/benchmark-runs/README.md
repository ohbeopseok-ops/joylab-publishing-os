# Browser Agent Benchmark Runs

This folder stores independently verified evidence reports for individual browser-agent benchmark runs. Raw benchmark scores are aggregated in `src/data/browser-agent-benchmark-scorecard.json`.
