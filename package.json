{
  "name": "joylab-publishing-os",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "dev": "node scripts/apply-curated-research-images.mjs && astro dev",
    "check:heroes": "node scripts/check-hero-quality.mjs",
    "prebuild": "node scripts/apply-curated-research-images.mjs",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "astro": "^7.3.2"
  },
  "devDependencies": {
    "sharp": "^0.35.4",
    "wrangler": "^4.130.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
